/**
 * Contains the type of subscriptions in the DataManager class
 */
export enum DataManagerSubscriptionType {
  VIEW_MODEL = 'view model',
  VALIDATION = 'validation',
  RULES = 'rules',
  FORM = 'form',
  SECTION = 'section',
  CATEGORY = 'category'
}
