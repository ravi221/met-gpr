/**
 * An enumeration to represent the context of where the plan filter is being used
 */
export enum PlanFilterContextTypes {
  CUSTOMER_HOME_PAGE = 'Customer Home Page',
  MAIN_MENU = 'Main Menu'
}
