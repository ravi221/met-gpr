import {Component, Input} from '@angular/core';
import {IPlan} from '../../plan-shared/interfaces/iPlan';

/**
 * Stub of {@link PlanContextMenuComponent}
 */
@Component({selector: 'gpr-plan-context-menu', template: ``})
export class PlanContextMenuStubComponent {
  @Input() plan: IPlan;
}
