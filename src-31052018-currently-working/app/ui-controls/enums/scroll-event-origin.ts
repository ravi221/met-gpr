/**
 * Used so subcribers to scroll events know where the originiating scroll event started
 */
export enum ScrollEventOrigin {
    MAIN_NAV_MENU = 'Main Navigation Menu',
    NOT_SET = 'NOT SET',
    AUTO_SEARCH = 'Auto Search Component'
}
