import {Component, Input} from '@angular/core';
import {INotification} from '../../../core/interfaces/iNotification';

/**
 * Stub of {@link NotificationBannerComponent}
 */
@Component({selector: 'gpr-notification-banner', template: ``})
export class NotificationBannerStubComponent {
  @Input() notification: INotification;
}
